# EduSphere Frontend Environment Configuration
# Copy this file to .env and update the values according to your setup

# API Configuration
# The base URL for your EduSphere backend API
VITE_API_BASE_URL=http://localhost:3000

# WebSocket Configuration  
# The WebSocket URL for real-time features (chat, notifications)
VITE_SOCKET_URL=ws://localhost:3001

# Application Configuration
# The name of your application instance
VITE_APP_NAME=EduSphere

# Version of your application
VITE_APP_VERSION=1.0.0

# Environment mode (development, production, test)
VITE_NODE_ENV=development

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the URLs to match your backend services
# 3. For production, use HTTPS/WSS protocols instead of HTTP/WS
# 4. Make sure your backend API is running on the specified URL
# 5. Ensure WebSocket server is accessible for real-time features